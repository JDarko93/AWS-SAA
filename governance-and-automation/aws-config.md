## AWS Config

- Is a service used for inventory resources like EC2 instances and apply conformance packs

- Conformance packs are collections of rules and rules are individual checks on services like EC2 instances to see if EBS volumes attached are encrypted or not
